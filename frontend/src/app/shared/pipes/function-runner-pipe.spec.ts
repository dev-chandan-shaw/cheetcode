import { FunctionRunnerPipe } from './function-runner-pipe';

describe('FunctionRunnerPipe', () => {
  it('create an instance', () => {
    const pipe = new FunctionRunnerPipe();
    expect(pipe).toBeTruthy();
  });
});
